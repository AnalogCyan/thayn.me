{
  "name": "thayn-me",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node build.js",
    "dev": "node build.js && npx serve public",
    "format": "prettier --write ."
  },
  "dependencies": {
    "front-matter": "^4.0.2",
    "handlebars": "^4.7.8",
    "marked": "^4.3.0"
  },
  "engines": {
    "node": ">=24"
  },
  "devDependencies": {
    "prettier": "^3.8.1"
  }
}
